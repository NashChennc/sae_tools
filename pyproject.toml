[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sae_tools"
version = "0.1.0"
description = "Analysis framework for SAE activations"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "torch>=2.6,<2.7",
    "transformer-lens>=3.1,<4",
    "sae-lens>=6.43,<7",
    "transformers>=4.57,<5",
    "huggingface-hub>=0.36",
    "hf-xet",
    "python-dotenv",
    "datasets",
    "pyyaml",
    "tqdm",
    "numpy",
    "pandas",
    "pyarrow",
    "scipy",
    "matplotlib",
    "seaborn",
    "ipywidgets",
    "pydantic>=2,<3",
    "jinja2>=3,<4"
]

[project.optional-dependencies]
dev = [
    "pytest"
]
workflow = [
    "snakemake>=8,<10"
]
tui = [
    "rich>=13",
    "textual>=0.58"
]

[project.scripts]
sae-tools-tui = "sae_tools_tui.__main__:main"
sae-tools-report = "sae_tools.reporting.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
